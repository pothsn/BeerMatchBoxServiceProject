@model IEnumerable<BeerMatchBoxService.Models.BreweryDBBeer>

@{
    ViewData["Title"] = "Index";
}

<h2>Beers</h2>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StyleName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Abv)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BreweryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.StyleName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Abv)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.BreweryName)
                </td>
                <td>
                    <a asp-controller="UserBeers" asp-action="AddUserBeer" asp-route-id="@item.Id">Add to taste profile</a>
                </td>
            </tr>
        }
    </tbody>
</table>
